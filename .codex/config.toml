# Project-scoped Codex subagent limits.
# Keep nesting shallow: the lead thread owns synthesis and all final decisions.
[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800
interrupt_message = true
