# One lead owns synthesis and integration while three direct workers use isolated worktrees.
# Keep nesting shallow so workers cannot create overlapping write ownership.
[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800
interrupt_message = true